[package]
name = "sticker-transformers"
version = "0.1.0"
authors = ["Daniël de Kok <me@danieldk.eu>"]
edition = "2018"

[dependencies]
failure = "0.1"
hdf5 = "0.5"
tch = "0.1"

[dev-dependencies]
approx = "0.3"
ndarray = { version = "0.13", features = ["approx"] }

[features]
model-tests = []